%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%                preamble                   %
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\usepackage[Conny]{fncychap}
\usepackage{geometry}
\usepackage[utf8]{inputenc}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{graphicx}
\usepackage{calc}
\usepackage{tikz}
\usetikzlibrary{positioning}
\usetikzlibrary{calc}
\usetikzlibrary{cd}
\usepackage{pgfplots}
\usepackage{fancyhdr}
\usepackage{titling}
\usepackage{verbatim}
\usepackage{bigints}
\usepackage{amsthm}
\usepackage{enumitem}
\usepackage{fmtcount}
\usepackage{datetime}
\usepackage[super]{nth}
\usepackage{ifthen}
\usepackage{xparse}
\usepackage{mathrsfs}
\usepackage{hyperref}
\usepackage{adjustbox}
\usepackage{xfp}
\usepackage{textcomp}
\usepackage{gensymb}
\usepackage{titlesec}
\usepackage{tocloft}

\tikzcdset{
  shorten >= 2pt,
  shorten <= 2pt,
  row sep/normal=2.4em
}

\tikzset{
  shorten <>/.style={ shorten >=#1, shorten <=#1 }
}

\pgfplotsset{compat=1.18}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%                  title                    %
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\def\internalclassname{}
\def\internaltypeofarticle{}
\newcommand{\classname}[1]{\renewcommand{\internalclassname}{#1}}
\newcommand{\typeofarticle}[1]{\renewcommand{\internaltypeofarticle}{#1}}
\author{Kim Doyoung, \\ Department of Computer Science \& Engineering, \\ Seoul National University}
\title{\internalclassname \\ \internaltypeofarticle}
\newdateformat{specialdate}{\monthname, \nth{\THEDAY}, \THEYEAR}
\date{Last updated at \specialdate\today. (UTC+9)}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%             chapter labeling              %
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\renewcommand{\thechapter}{\Roman{chapter}}
\renewcommand{\thesection}{\arabic{section}.}
\titleformat{\section}%           Command to format (section, subsection, etc.)
  {\normalfont\Large\bfseries}%   Format of the title
  {\thesection}%                  Section head spec
  {2mm}%                          Distance between the number and the title (empty = no space)
  {}%                             Code added before the title (empty = none)
\hypersetup{%                     hyperref package settings
    bookmarksnumbered=true,%      bookmarks show section numbers
    bookmarksopen=true,%          open bookmarks tree
    bookmarksdepth=2%             bookmarks depth (0: part, 1: chapter, 2: section, 3: subsection, etc.)
}
\renewcommand{\cftchapaftersnum}{.}%  Chapter number followed by a dot in TOC
\pdfstringdefDisableCommands{%        Disable commands for PDF bookmarks
  \def\thechapter{\Roman{chapter}.}%  Chapter number with a dot in bookmarks
}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%                  layout                   %
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\binoppenalty=10000%                  Prevent line breaks after binary operators (e.g. +, -, etc.)
\relpenalty=10000%                    Prevent line breaks after relational operators (e.g. =, <, >, etc.)
\setlength{\emergencystretch}{3em}%   Allow LaTeX to stretch lines a bit more to avoid overfull hboxes
\setlist[itemize]{leftmargin=*}%      Set itemize environment left margin globally
\setlist[enumerate]{%                 Set enumerate environment layout globally
  leftmargin=*,%      left margin
  labelsep=0.5em,%    space between label and item body
  align=left%         align label to left
}
\geometry{%                           page layout
  a4paper,%           paper size
  left=25mm,%         left margin
  right=25mm,%        right margin
  bottom=40mm%        bottom margin
}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%